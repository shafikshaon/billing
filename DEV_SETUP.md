# Developer Setup (Fix "vite: command not found")

This project uses:
- Node.js: >= 18.x
- npm (package manager)
- Vue 3.5.18 + Vue Router 4.4.5
- Vite 7.x

If you see `sh: vite: command not found` when running `npm run dev`, follow these steps.

## 1) Check Node and npm
